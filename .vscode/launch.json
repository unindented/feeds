{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Refresh",
      "request": "launch",
      "command": "pnpm run refresh ${cwd}/feeds.opml ${cwd}/feeds.sqlite3",
      "cwd": "${workspaceFolder}"
    },
    {
      "type": "node-terminal",
      "name": "Astro Dev",
      "request": "launch",
      "command": "pnpm run dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}
